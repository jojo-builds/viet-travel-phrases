# Korean Content Draft

This folder is the prep lane for a future South Korea / Korean SpeakLocal app variant.

Use it for:

- scenario review against the shared traveler baseline
- translation and pronunciation drafting
- South Korea-specific localization notes
- prep-only authoring artifacts that make the next translation pass faster
- audio planning after source text is stable

Current status:

- prep-only
- not runtime-wired
- now upgraded from a first translated batch into a second utility-first translation pack with `62` resolved rows plus an explicit `8`-row residual handoff in the current `70`-row seam

Current durable lane assets:

- `research/language-pipeline/korean/SOUTH-KOREA-TRAVEL-RESEARCH.md`
  - durable travel/use research script posture politeness stance and search implications
- `content-draft/korean/scenario-plan.json`
  - shared 10-scenario scaffold with South Korea-specific tip copy and quick-phrase emphasis
- `content-draft/korean/phrase-source.csv`
  - full 70-row prep-only source scaffold now carrying the original top-30 translated batch plus a second-pass utility pack across room problems convenience-store follow-ups directions cafe adjustments and other practical rows, with the remaining small-talk tail explicitly parked instead of left vague
- `content-draft/korean/first-wave-priority.csv`
  - full ranked execution ledger now showing the `62` resolved utility rows plus the explicit residual `63` to `70` handoff posture
- `content-draft/korean/source-notes.md`
  - authoring posture current counts residual-tail decisions and review-risk notes for the cleared two-pass draft
- `content-draft/korean/research-backlog.md`
  - compact native-review and defer-later handoff plus pronunciation and graduation blockers

Authoring posture for the next pass:

- keep the shared 10-scenario seam
- default to polite everyday Korean in safe service wording not casual `banmal`
- keep natural Hangul in `target_text`
- use `pronunciation` for traveler-friendly ASCII-friendly romanization support not as the only display layer
- preserve explicit wording-risk notes on goodbye route guidance bag or receipt requests room complaints and other context-sensitive service lines
- treat the remaining tail as `6` low-priority social follow-on rows plus `1` native-review social holdout and `1` deliberately deferred price-reaction row rather than as missing core utility
- keep the lane prep-only and out of `app/family` until translation pronunciation audio posture and non-Latin search review are real

Immediate next authoring target:

- review the flagged second-pass service route and room-problem rows with native or expert eyes before treating their wording as settled
- keep the practical translation queue closed instead of reopening low-value small talk by default
- leave `small-talk-7` for native review before any direct Korean translation attempt
- keep `asking-price-3` deferred until a Korea-fit price-reaction rewrite is worth doing
