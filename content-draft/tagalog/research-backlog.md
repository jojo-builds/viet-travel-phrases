# Tagalog Research Backlog

## Next authoring moves

- [ ] Start the next Tagalog content pass from `scenario-plan.json` -> `handoffContract`, especially `directPickupContract`, `runtimeHandoffAudit`, and `graduationBoundary`, and work from the `16` starter rows plus only the current `promote now` trio instead of reopening the whole earlier shortlist.
- [ ] Keep the current `24`-outcome contract intact: `16` starter rows, `1` deferred refusal row, `5` ordered next-pass expansion rows, and `2` later-only hold rows.
- [ ] Keep `tagalog-polite-basics-4` deferred before any promotion attempt.
  Reason: the rewrite is usable as an honesty boundary, but the line still needs native confirmation that it reads like a polite refusal to an offer or sales approach rather than a generic reassurance.
- [ ] Preserve the five-row direct-pickup contract and apply the current packet outcomes instead of flattening the whole set back into one vague reopen story:
  1. `tagalog-directions-1` - `promote now`; strongest map-showing bridge back into the starter directions pair.
  2. `tagalog-hotel-hostel-1` - `promote now`; reservation anchor for the hotel-arrival slice.
  3. `tagalog-hotel-hostel-2` - `promote now`; keep adjacent to reservation in the same slice.
  4. `tagalog-hotel-hostel-5` - `defer for native/expert review`; `aircon` wording still needs the stricter hotel-language review lane.
  5. `tagalog-convenience-store-6` - `keep prep-only`; useful payment follow-on, but keep it outside the immediate next slice.
- [ ] Keep this exact later-only hold set parked until the five-row direct-pickup set is either promoted or explicitly rejected:
  1. `tagalog-simple-problems-7` - escalation register risk is still higher than the stronger rows above it.
  2. `tagalog-grab-taxi-7` - the cash declaration is narrower than the card-payment check and should not leapfrog it.
- [ ] Normalize the final traveler-facing posture for reservation, check-in, aircon, and card wording before any promotion attempt.
- [ ] Keep `handoffContract.graduationBoundary` and `runtimeHandoffAudit` aligned with any future promotion or rejection decision so the runtime-facing lane can read one stop condition plus one trust audit instead of re-stitching the backlog and risk docs.
- [ ] Preserve the pickup cluster as one ordered review lane instead of splitting directions, hotel, and payment into separate reopen decisions too early.
- [ ] Settle a stronger pronunciation contract only after the promoted core survives native or expert review.

## Review gates still needed later

- [ ] Native or expert review for politeness defaults and respectful acknowledgment usage around `paumanhin`, `opo`, and service-tone requests.
- [ ] Refusal-tone review for `tagalog-polite-basics-4`, specifically whether `Ayos lang salamat` lands as a polite decline in sales or offer contexts.
- [ ] Hotel-arrival and room-issue review for reservation, check-in, and aircon wording.
- [ ] Payment review for card and cash wording in convenience-store and ride scenarios.
- [ ] Native confidence check on the rewritten generic directions baseline before it returns to the active handoff.
- [ ] Escalation review for `tagalog-simple-problems-7` if it is ever reopened.
- [ ] Pronunciation review for mixed-English and loanword-heavy rows before any traveler-facing freeze.

## Graduation blockers before runtime wiring

- [ ] A reviewed promoted core that survives expert or native validation
- [ ] A validated row-by-row decision on whether the current `promote now` trio really survives review and whether the deferred/prep-only pickup rows should stay where they are
- [ ] A stronger pronunciation contract
- [ ] An honest audio posture for the expanded Tagalog lane
- [ ] Durable validation evidence that the first-wave cleanup reduced risk instead of just reshuffling rows
