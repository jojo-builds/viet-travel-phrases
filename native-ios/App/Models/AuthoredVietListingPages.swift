import Foundation

enum AuthoredVietListingPages {
    static let all: [PhraseDetailPage] = []
    static let bundledTierOneFamilyCount = 0
    static let bundledMainPageCount = 0
    static let bundledChildPageCount = 0
    static let bundledPageIDs: Set<String> = []

    static func categoryIDs(for pageID: String) -> [String]? {
        nil
    }
}
