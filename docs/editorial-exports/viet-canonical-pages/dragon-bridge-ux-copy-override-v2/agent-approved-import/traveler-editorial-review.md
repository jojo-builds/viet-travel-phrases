# Traveler / Editorial Quality Review

Task: `TASK-VIET-DRAGON-BRIDGE-LANDMARK-UX-OVERRIDE-001`

Reviewer: read-only agent gate

Outcome: APPROVE

The proposed Dragon Bridge override reads human, practical, and first-time-traveler oriented. The order works as a traveler task flow: `Start here`, `Say the name`, `Getting there`, `At the bridge`, `Meeting or pickup`, `What the name means`, `Good to know`, and `Nearby needs`.

Approved details:

- `Getting there` groups `Đi cầu Rồng` and `Cầu Rồng ở đâu?`, which match the first task.
- `At the bridge` with the photo-help row is natural and useful.
- `Meeting or pickup` is the right place for `Cho tôi xuống gần Cầu Rồng.` and `Dừng ở Cầu Rồng`.
- `Nearby needs` correctly demotes ATM and food rows so they do not distract from the core landmark actions.
- The visible patch copy avoids internal/model wording such as `anchor`, `route phrase`, `where-question`, `connect to`, and `this page`.

Non-blocking note: `At the bridge` says `photo or direction`, while the section only has a photo row. Direction help is covered nearby in `Getting there`, so this was not a blocker.
