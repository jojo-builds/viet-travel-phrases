# Live Rendered-Page Review

Task: `TASK-VIET-DRAGON-BRIDGE-LANDMARK-COPY-IMPORT-001`

Outcome: `APPROVE_WITH_FOLLOW_UP`

Issue classification:

- `FOLLOW_UP`: Dragon Bridge still uses the generic Vietnam masthead. This is accepted for the copy import per the task card and should become `TASK-VIET-DRAGON-BRIDGE-HERO-ASSET-001`.
- `ACCEPTED_TEMPORARY_RISK`: The incoming patch uses stale `viet-phrase-*` exported page IDs for city-library authored pages. The import resolves by `phrase_id` and preserves the current canonical authored page ID.

Rendered-page readout:

- Top page shows `Cầu Rồng` with English subtitle `Dragon Bridge`; the generic masthead remains visible and is documented as visual follow-up.
- The page reads as traveler tasks, not as a generic place/article page.
- Visible order is `Start here`, `Say the name`, `Getting there`, `At the bridge`, `Meeting or pickup`, `What the name means`, `Good to know`, and `Nearby needs`.
- `Say the name` contains the current-page self row without a chevron destination.
- `Getting there` groups route and where rows.
- `At the bridge` contains the photo-help row.
- `Meeting or pickup` groups drop-off and stop rows.
- `Nearby needs` keeps ATM and food rows after the core landmark actions.
- Rendered Dragon Bridge copy has zero hits for: `anchor`, `place name`, `useful moments`, `where-question`, `route phrase`, `connect to`, `this page`, and `relationship rows`.

Screenshot proof:

- `docs/task-results/assets/TASK-VIET-DRAGON-BRIDGE-LANDMARK-COPY-IMPORT-001/dragon-bridge-top.jpg`
- `docs/task-results/assets/TASK-VIET-DRAGON-BRIDGE-LANDMARK-COPY-IMPORT-001/dragon-bridge-mid.jpg`
- `docs/task-results/assets/TASK-VIET-DRAGON-BRIDGE-LANDMARK-COPY-IMPORT-001/dragon-bridge-lower.jpg`
