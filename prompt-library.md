# Prompt Library - Historical Vietnamese Single-App Prompts

This file is archived context from the original Vietnamese single-app build. It is not the authoritative prompt source for current SpeakLocal family work.

For current family sessions:
1. Start from `docs/DECISIONS.md` and `docs/PRIORITIES.md`.
2. Use `docs/operations/*` as the live operational workflow.
3. Treat `E:/AI/SpeakLocal-App-Family` as the canonical repo root.
4. Use Windows authoring plus manual iPhone testing and EAS preview/release builds.
5. Do not assume any supported macOS or simulator lane.

If you intentionally need to study or replay the old Viet-only build sequence, treat the historical prompts as archival reference only and do not let them override the family manifests, registry, or operational docs.
