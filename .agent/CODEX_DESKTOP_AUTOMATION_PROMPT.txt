Repo-local queue run only.
Use `/Users/jojolim/Developer/products/speaklocal/app-family` as the canonical root.
Treat Windows paths in old task artifacts as migration/archive references only.
Ignore broad family startup and do not use `$orchestrate-speaklocal-family` during queue startup.
This prompt is for already-shaped execution tasks only, not for high-level roadmap intake, broad feature triage, or backlog creation.
Treat the external visual tracker / orchestrator lane as the place where new feature ideas are shaped before they ever enter the queue.
This prompt is only a bootstrap. The actual assignment must live in the claimed task's `spec.md`.

Read, in order:
1. `AGENTS.md`
2. `.agent/README.md`
3. `.agent/QUEUE_START.md`
4. `.agent/AUTOMATION.md`
5. `.agent/TASK_PROMPTING.md`

Queue flow:
- confirm this worker exposes a callable `functions.spawn_agent`
- prefer helper-backed claim with `SPEAKLOCAL_REVIEW_RUNTIME=subagents python3 .agent/queue_tool.py claim-next --session-id "<session-id>" --label "<label>" --lease-minutes 180`; this enforces active write-lock conflicts before claim
- if helper-backed claim is unavailable, use `.agent/coordination/queue-index.json` only as a shortlist: try `reclaimable`, then `queued`; for each candidate, read live `state.json`, skip stale entries and active write-lock conflicts, claim by direct `state.json` patch, and re-read immediately to confirm ownership
- if no candidate can be claimed, stop honestly instead of looping

After claim:
- ignore queue-index and locks unless the task is explicitly queue maintenance
- read only `spec.md` and task-required files, interpret the spec as an outcome contract, stay inside task scope, and heartbeat/re-read the claimed `state.json` on material phase changes, every `10` to `15` minutes during long work, before/after spawned subagent waits, and before finish
- for meaningful 3-gate tasks, follow the lean review contract in `.agent/AUTOMATION.md`: reviewers return judgment text only, the parent writes the 4 review artifacts in one patch, and harvested reviewer agents are closed promptly
- write `result.md`, include `Process feedback`, require latest-pass `Approval: APPROVE` or `Approval: BLOCK`, and finish by patching `state.json` directly to `done` or `blocked` with `result.md` agreeing

Process one task only, then stop.
