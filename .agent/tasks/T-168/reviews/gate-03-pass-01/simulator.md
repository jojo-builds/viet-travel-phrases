# Gate 3 Pass 1 - Simulator

Approval: APPROVE

Reviewer lane: simulator proof.

Evidence:
- `simulator-proof.md` records the DEBUG SQLite launch switch.
- The proof documents Home/Search -> `Xin chao` -> related `Cam on` -> back/forward.
- Six PNG artifacts exist and match the documented sequence.
- DB spot-check confirmed `viet-phrase-polite-1 -> viet-phrase-polite-2` and audio keys `polite-1` / `polite-2` resolve to bundled files.
