The plan is directionally strong and I would pass it at Gate 1. The biggest reason is that it targets the real depth gap rather than only raw expansion: the current answer-page sample already shows money, hotel, and food hubs forced through the same `practical-service-navigation-v1` scaffold in `answer-page-sample-v1.json`, so adding dedicated `money-transaction`, `hotel-accommodation`, and `food-drink` mixes is the right way to make flagship pages feel less generic. Pairing that with 15 explicit flagship rewrites hits the exact pages most likely to feel thin today.

The biggest depth risk is the 30 planned promotions from `relation-sample-v1.json`. Right now those candidates are uniformly skeletal: almost all are relation-only clusters with just one repair rail and one ask-next rail, no real likely-reply depth, and many only have a single phrase row in `phrase-source.csv`. If they are promoted mechanically, this becomes count inflation. The plan still clears Gate 1 because it explicitly couples those promotions with 120+ new support markers and flagship rewrites, but execution has to treat promoted hubs as real enrichment work, not template conversion, and there is very little numeric slack since `50 -> 80` lands exactly on the floor.

Approval: APPROVE
