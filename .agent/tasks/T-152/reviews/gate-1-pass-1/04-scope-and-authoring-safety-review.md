Finding: No write-scope or schema blocker is visible; the additive sidecar contract can support 3 new phrase classes and 30 promoted hubs without inventing a new model. The blocker is authoring safety in the `notes` seam. The contract still says this layer must stay lightweight and must not become a mini graph database, but current hot rows in `phrase-source.csv` already carry very high answer-page marker density. A 30-hub / 120+ support-row expansion using the same unrestricted reuse pattern is likely to push the notes field past the stated safety boundary and make relation/answer drift harder to audit. I would only approve after the implementation plan adds an explicit guardrail for marker density on already-saturated rows while staying inside the current sidecar model.

Approval: BLOCK
