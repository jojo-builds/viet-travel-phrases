Gate 2 pass 1 traveler utility review for T-114.

The handoff is materially stronger for traveler retrieval: the 24-row contract is now explicit in the CSVs, the relation sample separates starter, deferred, pickup, and later-only paths, and the sidecar keeps real status distinct from follow-on class so downstream work can recover the next move without re-deriving the map. It also stays appropriately prep-only and does not overclaim runtime readiness.

Approval: APPROVE
