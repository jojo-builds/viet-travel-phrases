Gate 1 pass 1 retrieval handoff review for T-114.

The pre-edit baseline is clear enough. The CSV, JSON, and narrative handoff surfaces agree on the same 16 starter rows, 1 deferred boundary, 5 ordered pickup rows, and 2 later-only holds, and they keep row status separate from follow-on class. The main edit pass is well-scoped because it stays prep-only and has one explicit expansion cluster plus one explicit refusal boundary.

Approval: APPROVE
