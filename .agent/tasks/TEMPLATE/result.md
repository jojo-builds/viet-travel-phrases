# Result: T-XXX

## Status
- in_review | done | partial | blocked
- use `in_review` while the final gate is still open; switch to `done` only when `state.json` is finalized too

## Truth changed
- live | prepared-next | planning | future-option

## Changed files
- `path/to/file` - one-line reason

## Validation
- `command here` - passed | failed | not run

## Notes
- max 5 concise bullets

## Blockers
- max 5 concise bullets

## Reviews
- reviewer artifact paths if used

## Logs
- log artifact paths if used

## Process feedback
- Each bullet must start with exactly one of: `BUG`, `SUGGESTION`, or `NONE`
- Example: `- SUGGESTION: add explicit reclaim event logging so operators can see when a stale task became a fresh claim`
- max 3 concise bullets about unnecessary context, confusing process steps, avoidable reasoning overhead, or workflow/tooling improvements discovered during this run

## Recommended next step
One short paragraph.
