Approval: APPROVE
Findings:
- none
Gate recommendation: Advance Gate 2; the flagged fee/wait/access rows are now off the `likelyReply` rails, `viet-service-card` correctly keeps `v1000-money-cash-only` as the plausible immediate reply, and the four hubs now read as believable traveler flow with follow-up/repair support where it belongs.
