**Findings**
- The recovered branch still preserves the claimed packet shape: `phrase-source.csv` is at `196` total rows, with `63` substantial families and `126` substantial rows, matching the T-139 recovery truth.
- Variant discipline is intact in the recovered substantial packet. Every substantial family remains a strict two-row family with exactly one `say-first` row plus one secondary row, and every secondary role stays inside the fixed compact set: `clearer`, `more-polite`, or `also-common`.
- The recovery also still aligns with the planning surfaces: `first-wave-priority.csv` has `63` unique families, and `tagalog-v2-first-wave.csv` matches the same `63` recovered families, so there is no visible drift that weakens family boundaries before validation.

**Approval**
- No concrete variant-discipline break is visible in the recovered substantial packet that should stop T-141 from proceeding to validation.

Approval: APPROVE
