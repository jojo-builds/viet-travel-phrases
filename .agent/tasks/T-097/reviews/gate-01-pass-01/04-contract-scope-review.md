# Gate 1 Pass 01 - Contract Scope Review

Approval: APPROVE

The plan stays inside the task contract: it works only in `content-draft/turkish/**` plus the task audit/log artifacts, it does not touch runtime wiring, and it explicitly handles the tricky holdouts by deferring the non-natural utensils row instead of forcing a fake translation. The residual-queue cleanup is also well-formed: the bargaining rows are kept visible but demoted, the medical and localization-sensitive rows stay flagged, and the named docs are the right surfaces to make the smaller queue explicit.
