# Gate 3 Pass 1: V1 / V2 Scope

Judgment: The V1/V2 scope is explicit and realistic. The README separates before-live, SQLite-runtime, and practice/mascot acceptance work. `native-ios/project.yml` confirms `LanguagePacks` is excluded, so the SQLite wait is grounded. Practice and mascot are correctly treated as non-V1 until design/assets/runtime exist.

Approval: APPROVE

Blocking findings: none.
