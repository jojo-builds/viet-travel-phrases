Approval: BLOCK

- The current snapshot stays within the French prep lane and avoids forbidden surfaces, so scope is fine.
- The next pass must produce the spec-required `T-119` outputs: `.agent/tasks/T-119/logs/french-residual-packet.md`, `.agent/tasks/T-119/result.md`, and exactly four review files per gate.
- Keep all edits strictly inside `content-draft/french/**` and `.agent/tasks/T-119/**`.
- Keep the work prep-only with no `app/**`, `site/**`, `ops/**`, or `docs/operations/**` edits, and preserve explicit residual decisions for `small-talk-7` and `coffee-shop-4`.
