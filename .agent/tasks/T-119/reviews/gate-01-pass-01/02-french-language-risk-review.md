Approval: APPROVE

- The residual handoff is internally consistent: `README.md`, `source-notes.md`, `research-backlog.md`, and `first-wave-priority.csv` all agree that ranks `63` through `68` are later-batch social rows, `69` stays deferred-native-review, and `70` stays rewrite-before-translation.
- French register risk is honestly stated: the lane defaults to France-fit, polite, `vous`-safe service wording, and the notes keep pronunciation as a helper only, not a substitute for real French.
- Native-review risk is not being downplayed; the docs keep transit, payment, room-problem, medical, and bill wording visibly flagged instead of pretending they are settled.
- The weak France-fit cafe row is handled correctly: `french-coffee-shop-4` remains explicitly parked for rewrite or retirement rather than being quietly promoted.
- No must-fix issue stands out in the pre-edit snapshot; the next pass only needs to preserve the same explicit boundary discipline.
