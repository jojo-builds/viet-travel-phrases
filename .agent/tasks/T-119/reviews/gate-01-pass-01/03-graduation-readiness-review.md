Approval: BLOCK

- The residual rows are explicit, but the graduation boundary is still spread across multiple docs; `README.md`, `source-notes.md`, and `research-backlog.md` still read like a work-in-progress handoff instead of one compact packet.
- The six later-social rows are parked, but the packet does not yet make the social cutoff unambiguous enough for future planning; the next pass should state whether they are permanently later-only or just deferred for the next pass.
- `french-small-talk-7` and `french-coffee-shop-4` are correctly singled out, but they remain open decisions rather than closed lane policy, so the residual debt is still partly procedural.
- Alias/search posture and audio posture can remain blockers, but the lane needs one compact packet so future planning does not have to reopen older context first.
