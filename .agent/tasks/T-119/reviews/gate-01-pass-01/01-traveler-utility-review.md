Approval: APPROVE

- The residual is genuinely bounded: the lane now has a clear six-row later-social pocket, one native-review social row, and one rewrite-only cafe row in the task spec and current French lane artifacts.
- The French docs agree on the same next move and do not drift into a broad second-wave reopen.
- The traveler-value ordering is sane: utility rows are already resolved, while low-value social rows are explicitly parked instead of being mixed back into the core pack.
- The one must-hold item is to keep `french-coffee-shop-4` resolved as a single explicit rewrite-or-retire decision, not a reopened mini-batch.
- The snapshot is suitable for a bounded closure pass because it preserves prep-only scope and keeps the remaining ambiguity narrow.
