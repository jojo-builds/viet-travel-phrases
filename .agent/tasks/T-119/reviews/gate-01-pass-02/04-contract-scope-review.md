Approval: APPROVE

- The proposed split stays inside the contract: it converts the residual into one bounded graduation-boundary packet instead of reopening a broad translation batch.
- Classifying `french-small-talk-1` through `french-small-talk-6` as later-only social holds matches the spec's explicit residual adjudication requirement and preserves prep-only scope.
- Keeping `french-small-talk-7` native-review-only is consistent with the existing review-sensitive posture and avoids forcing a weak-fit social row into the core pack.
- Retiring `french-coffee-shop-4` from the current lane unless a future cafe-fit rewrite reintroduces it is the right scope-control move; it prevents a low-value row from expanding the current pass.
- The plan cleanly supports the lane's graduation boundary without touching runtime or widening into unrelated content work.
