Approval: APPROVE

- The proposed boundary matches the current lane truth: `small-talk-1` through `small-talk-6` are already parked as later social coverage, so labeling them as later-only holds is a clean graduation wrapper, not scope creep.
- `small-talk-7` is already the explicitly native-review-only row, so keeping it separate preserves the language-risk boundary instead of mixing it back into the graduated pack.
- `coffee-shop-4` is correctly treated as the lone weak France-fit outlier; retiring it unless a future rewrite reintroduces it prevents a fake "one more row" reopen.
- The plan keeps the residual compact and legible for future planning, which is the right signal for graduation readiness.
- There is no graduation-boundary reason to keep this in pre-edit review after the proposed split is applied.
