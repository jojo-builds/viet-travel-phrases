Approval: BLOCK. The raw bar is met (50 hubs, 95 distinct support rows, hub/cluster anchors and target phrase IDs all resolve), but the packet is not fully sidecar-consistent: `viet-greeting-thank-you`, `viet-urgent-ambulance`, and `viet-medical-trouble-breathing` still inherit/reference `crossClassExit` from their answer-page module mix / module `relationRefs`, while `relation-sample-v1.json` gives those clusters `crossClassExit: []` and `phrase-source.csv` has no matching `answer-page-sample=...:support:crossClassExit` markers. That leaves three hubs internally inconsistent across the answer-page sample, relation sidecar, and CSV, so I see a blocker-level structural issue and cannot approve pass 11.
