Approval: BLOCK. The packet clears the raw bar (`50` hubs, `95` distinct support rows, and the relation target phrase IDs resolve), but the blocker-level structural mismatch from pass 11 is still present at the module-mix contract layer: `greetings-social-v1` and `urgent-help-medical-v1` still declare `crossClassExit` as a required relation bucket, while `viet-greeting-thank-you`, `viet-urgent-ambulance`, and `viet-medical-trouble-breathing` omit that bucket in the hub packet, have `crossClassExit: []` in `relation-sample-v1.json`, and have no matching `answer-page-sample=...:support:crossClassExit` markers in `phrase-source.csv`. That leaves the packet numerically complete but not fully sidecar-consistent, so I cannot approve pass 12.
