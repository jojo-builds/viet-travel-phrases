Approval: APPROVE

`T-151` stays inside its content-lane contract: the spec is still a bounded Viet answer-page/rerun task, `answer-page-sample-v1.json` remains additive with `hubCount: 50`, `phraseClassCount: 4`, and `supportingRowCount: 95`, both sidecars explicitly keep phrase wording truth in `phrase-source.csv` and relation truth in `relation-sample-v1.json`, and the CSV still uses the existing header with membership carried only in `notes` markers (`answer-page-sample=` / `relation-sample=`) rather than new transient columns or review metadata. The pass-12 blocker is resolved in the allowed packet itself: `viet-greeting-thank-you`, `viet-urgent-ambulance`, and `viet-medical-trouble-breathing` now show empty `crossClassExit` buckets in `relation-sample-v1.json`, which matches the stated module-layer fix and does not introduce new scope or unsafe authoring drift.
