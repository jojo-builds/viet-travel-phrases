The working pass is contract-compliant within the reviewed packet: the Japan prep lane stays closed and prep-only, and the residual graduation ledger is explicit instead of ambiguous.

- `first-wave-priority.csv` reconciles cleanly to the stated 47-row total, with the required `promote` / `later-only` / `native-review-only` / `rewrite-needed` / `retire` outcomes all represented.
- `README.md`, `source-notes.md`, `research-backlog.md`, and the graduation packet agree on the 34-row starter handoff and the bounded residual tail.
- I do not see scope creep in the reviewed files; the packet stays in `content-draft/japanese/**` and `.agent/tasks/T-122/**`, with runtime wiring explicitly kept out.

Approval: APPROVE
