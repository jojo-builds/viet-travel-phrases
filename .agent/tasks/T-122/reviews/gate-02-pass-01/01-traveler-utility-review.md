The Japan graduation packet is traveler-useful and internally consistent: the 34 promoted rows form a practical starter handoff, and the remaining 13 rows are cleanly partitioned so they do not leak back into active starter debt.

- `content-draft/japanese/README.md`, `source-notes.md`, and `.agent/tasks/T-122/logs/japan-graduation-packet.md` all agree on the same graduation boundary and the same "prep-only, not runtime-wired" posture.
- The residual outcomes in `first-wave-priority.csv` are sensible for utility: medical stays isolated, weak bargaining and social filler stay out of the starter set, and the rewrite-needed rows are blocked rather than diluted into the handoff.
- The packet now reads like a compact planning artifact future work can use directly without reopening `T-101`.

Approval: APPROVE
