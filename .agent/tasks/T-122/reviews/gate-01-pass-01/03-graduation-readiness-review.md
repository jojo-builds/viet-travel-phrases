The lane is already shaped in the right direction for a compact graduation-boundary packet: T-101 left a bounded starter slice, the tail is explicitly bucketed, and the docs consistently keep the lane prep-only and not runtime-wired. The main pass still needs to make the handoff boundary unmistakable in one place so future planning can read the starter packet, the deferred tail, and the non-goals without having to reconstruct T-101.

- `research-backlog.md` still reads like a live worklist; the pass should tighten it into a compact "what remains and why it stays out" note instead of a broad backlog.
- The status vocabulary is now explicit but spread across files; the pass should make the mapping between `promoted core`, `later-review`, `deferred-after-starter-slice`, and `deferred-rewrite-needed` obvious at a glance.
- The keep-out boundary for medical, bargaining, and social rows needs to be stated as a hard graduation fence, not just a priority note, so the starter slice cannot expand by implication.

Approval: APPROVE
