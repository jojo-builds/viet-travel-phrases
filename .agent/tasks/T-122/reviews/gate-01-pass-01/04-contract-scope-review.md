The lane is still within the allowed Japan prep surfaces, but it does not yet satisfy the contract shape for this gate. The current artifacts still read like an active next-pass queue rather than a compact graduation-boundary packet, and the required residual-outcome vocabulary is not fully normalized, so I cannot approve this pre-edit state.

- The contract asks for explicit `promote`, `later-only`, `native-review-only`, `rewrite-needed`, and `retire` outcomes; the current files mostly use `first-wave-promoted-core`, `later-review`, `deferred-after-starter-slice`, `deferred-rewrite-needed`, and `first-wave-flagged-holdout`, which leaves the residual taxonomy incomplete.
- `research-backlog.md` still contains open checklist items and "graduation blockers" language, so the lane is not yet shaped as a finished handoff packet.
- Scope drift is not the problem here; the issue is that the pre-edit documentation still preserves backlog posture instead of the sharper residual boundary the task contract requires.

Approval: BLOCK
