The revised packet now satisfies the gate contract shape. The lane is presented as a closed, prep-only graduation-boundary packet, and the residual outcomes are now explicit and consistent across the handoff docs and the authoritative ledger.

- `content-draft/japanese/first-wave-priority.csv` now uses the required residual vocabulary: `promote`, `later-only`, `native-review-only`, `rewrite-needed`, and `retire`.
- `README.md` and `source-notes.md` agree on the 34-row promoted starter handoff and the bounded residual set instead of reading like an open translation queue.
- `research-backlog.md` still has follow-up items, but they are clearly post-closeout guards rather than unresolved scope debt.

Approval: APPROVE
