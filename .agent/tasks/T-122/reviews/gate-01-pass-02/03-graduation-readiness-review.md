The revised packet now reads as a compact graduation-boundary handoff: the promoted starter set is clearly separated from later-only, native-review-only, rewrite-needed, and retired rows, and future planning can follow the boundary without reopening `T-101`.

- The authoritative split is consistent across `README.md`, `source-notes.md`, `first-wave-priority.csv`, and the graduation packet log.
- The non-goals are explicit: medical, rewrite-needed, and retired social rows stay outside starter debt.
- `research-backlog.md` still has checklist structure, but it functions as a closeout note rather than a live queue, so it does not break the handoff.

Approval: APPROVE
