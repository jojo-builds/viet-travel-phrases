Approval: APPROVE

Role: Spain-fit risk review

Findings:
- No blocking Spain-fit risk in the current pass. The packet keeps the practical traveler rows promoted, keeps the social tail explicitly out of starter planning, and isolates the medical row as `native-review-only`.
- The prep-only boundary is still intact: the lane remains not runtime-wired, and the residual ledger is framed as handoff material rather than a broad translation reopen.

Recommended changes:
- Keep the current residual contract stable and do not reopen the tail sweep unless product scope changes.
- Preserve the explicit separation between `promote`, `later-only`, `retire`, and `native-review-only` so the Spain-fit boundary stays readable in future planning.
