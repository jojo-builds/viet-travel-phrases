Approval: APPROVE

Role: Spain-fit risk review

Findings:
- No blocking Spain-fit risk. The packet keeps the prep-only boundary explicit, preserves the practical traveler rows as promoted, and keeps the social tail outside starter planning.
- The medical holdout remains clearly isolated as `native-review-only`, so the draft does not blur into runtime-ready or medically broadened scope.

Recommended changes:
- Keep the current residual contract stable through closure.
- Do not reopen the tail sweep unless product scope changes.
- Preserve the explicit separation between `promote`, `later-only`, `retire`, and `native-review-only`.
