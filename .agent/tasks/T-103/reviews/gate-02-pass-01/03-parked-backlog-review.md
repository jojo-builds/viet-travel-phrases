The edited surfaces do resolve the parked cluster cleanly: all 7 parked rows now have explicit row-level dispositions, split into a 5-row next-pass pickup set and a 2-row later-only hold set, and that split is visible in the CSV notes as well as the narrative docs. The lone deferred refusal row remains separately and explicitly deferred, so the lane no longer reads as a fuzzy backlog bucket.

Approval: APPROVE
