## Summary
- The French scaffold is tight enough for the next authoring pass.
- The lane now has a concrete 70-row source scaffold, a ranked 30-row first wave, and review flags that keep the sensitive rows visible without blocking the starter pack.
- Register posture, rewrite decisions, and risk labeling now read as an executable handoff.

## Checks
- `phrase-source.csv` is populated, uses prep-only blank `target_text` and `pronunciation`, and keeps the one weak-fit `coffee-shop-4` row explicitly out of translation.
- `first-wave-priority.csv` is ranked and has 30 `first-wave-ready` rows with usable `review_flag` and `priority_reason` guidance.
- `README.md`, `source-notes.md`, and `research-backlog.md` agree on polite, `vous`-safe, France-focused traveler phrasing and on staying prep-only.
- The review flags are specific enough for the next pass: medical, transit, bill or payment, room-problem, and service-tone rows are all called out instead of being left implicit.

## Risks
- Pronunciation is still intentionally unresolved, so the next authoring task still needs one consistent ASCII-helper rule before that column is filled.
- `coffee-shop-4` still needs rewrite or retirement before translation, but it is correctly quarantined.

## Approval
- The register posture, rewrite decisions, and row-level review flags are tight enough for the next French authoring pass.

Approval: APPROVE
