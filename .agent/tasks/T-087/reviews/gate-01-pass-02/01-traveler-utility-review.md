## Summary
- Gate 1 traveler-utility concerns are resolved enough to advance.
- The French lane now has the missing authoring scaffold, a ranked first-wave queue, and row-level guidance that makes the next translation pass immediately actionable instead of requiring another orientation step.
- The original planning-gap blocker is gone.

## Checks
- `content-draft/french/README.md` now states the lane is prep-only but authoring-ready.
- `content-draft/french/source-notes.md` now has row-level rewrite decisions, register guidance, and review-sensitive buckets.
- `content-draft/french/phrase-source.csv` exists and is populated with the France-fit scaffold.
- `content-draft/french/first-wave-priority.csv` exists and is populated with a ranked starter queue.
- `content-draft/french/research-backlog.md` now points directly at the next translation moves.
- `content-draft/french/scenario-plan.json` still parses and preserves the shared 10-scenario seam.
- The top of the queue is concrete: polite openers, repair phrases, hotel arrival, payment, station help, and bill handling are prioritized.

## Risks
- The lane is still prep-only, with no actual French translations or pronunciation coverage yet.
- Some rows remain explicitly flagged for later review, especially medical, transit, and service-tone cases.
- `coffee-shop-4` is still a weak-fit row and remains a rewrite-or-retire candidate.

## Approval
- The original Gate 1 blocker was missing scaffold truth; that blocker is gone. The lane is now safe to move past the initial planning gap.

Approval: APPROVE
