## Summary
- The French prep package is contract-scope ready to finalize.
- The required scaffold files are present, the lane stays prep-only, and the latest Gate 1 and Gate 2 evidence is unanimously approved.
- `result.md` is intentionally still `in_review` for this final review pass.

## Checks
- `README.md`, `source-notes.md`, `scenario-plan.json`, `phrase-source.csv`, `first-wave-priority.csv`, and `research-backlog.md` are all present and aligned on the same prep-only handoff shape.
- `phrase-source.csv` and `first-wave-priority.csv` are populated and non-empty, with a concrete ranked starter pack for the next translation pass.
- `scenario-plan.json` still preserves the shared 10-scenario seam.
- Latest Gate 1 and Gate 2 passes are unanimous approval sets.
- The task stays within `content-draft/french/**` and `.agent/tasks/T-087/**`, with no runtime wiring involved.

## Risks
- `pronunciation` coverage is still intentionally deferred, so the next pass must apply one consistent helper rule.
- `coffee-shop-4` remains the lone weak-fit row and should be rewritten or retired before translation.
- Several medical, transit, room-problem, and service-wording rows remain correctly flagged for later review, but that is expected and visible.

## Approval
- The package is ready to finalize to `done` from a contract and scope perspective.

Approval: APPROVE
