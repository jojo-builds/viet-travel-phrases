# Gate 1 Pass 1: Tagalog Language Risk Review

Approval: APPROVE

- The `16 / 5 / 1` handoff is internally consistent across `README.md`, `source-notes.md`, `first-wave-priority.csv`, and `tagalog-v2-first-wave.csv`.
- The loanword-heavy hotel, payment, and map-showing rows are still kept out of the core where they belong, and the refusal row remains honestly deferred.
- Pronunciation risk is handled conservatively as helper-only guidance rather than a frozen contract.
- The core still carries a few explicit review flags, so the next pass should treat the `16` core rows as starter-ready, not risk-free.

