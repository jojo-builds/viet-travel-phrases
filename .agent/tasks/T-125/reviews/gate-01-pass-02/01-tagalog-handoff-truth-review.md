Approval: APPROVE

Findings
- None blocking. The planned packet makes all five direct-pickup rows explicit, preserves the deferred refusal row, preserves the later-only holds, and keeps the lane prep-only.

Required fixes before Gate 1 pass 2 can approve
- None.

Notes
- Mirror the same row labels and outcome words across `scenario-plan.json`, `README.md`, and `source-notes.md` to avoid consistency drift.
