Approval: BLOCK

Findings
- The planned packet still needs an explicit per-row outcome layer encoded in the contract data rather than only shared pickup labels.

Required fixes before Gate 1 pass 3 can approve
- Encode exact per-row outcomes for the five direct-pickup rows in the source-of-truth packet files.
- Keep the refusal boundary and later-only holds unchanged.

Notes
- The overall prep-only safety posture is already consistent.
