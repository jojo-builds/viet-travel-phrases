# Gate 1 Pass 1: Relation Depth Review

Approval: BLOCK

## Summary

The hub set is strong, but the current plan does not yet guarantee multi-step typed relation depth across likely replies, repair branches, and cross-class exits.

## Findings

- The notes still describe generic linked families rather than typed relation buckets.
- `practical-service-navigation` needs an explicit likely-reply / operator-question branch.
- Urgent-help-medical hubs need required detail, repair, and escalation branches so the flow does not stop after the opener.
- There is no explicit relation-depth minimum per enriched hub yet.

## Suggested adjustments

- Require typed buckets such as `bestDefault`, `quickSay`, `alternate`, `likelyReply`, `repairIfMissed`, `askNext`, and `escalateTo`.
- Add a relation-depth rule for every enriched hub: alternate, likely-reply, repair, and at least one cross-class exit.
