Approval: APPROVE

Rationale: The audio-proof chain is still honest: the validator now checks direct parity with `content-draft/viet/website-preview.json`, confirms the manifest/public copies match, and requires every `audioStatus=ready` phrase to resolve under `/data/phrase-audio/*`; `site/scripts/phrase-module-loader.js` only consumes that exported truth. I do not see a remaining evidence gap that would make this unsafe to mark done.
