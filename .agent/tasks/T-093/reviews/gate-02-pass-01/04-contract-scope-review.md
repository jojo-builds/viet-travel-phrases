Approval: APPROVE

Rationale: The work stayed inside the export seam: the audit says the manifest copies were identical and left unchanged, while the only functional hardening was in `scripts/website/Test-SpeakLocalWebsiteArtifact.ps1` to assert `content-draft/viet/website-preview.json` parity on module count and `(moduleId, scenarioId)` pairs. I do not see unrelated app/ops churn.
