Approval: APPROVE

Rationale: The audio reference chain is tight and deterministic: the validator now checks `content-draft/viet/website-preview.json` parity directly, the manifest copies agree, ready clips are required under `/data/phrase-audio/*`, and `site/scripts/phrase-module-loader.js` consumes that exported manifest path. I do not see an unresolved audio-reference defect blocking immediate finalization once this review is harvested.
