Approval: APPROVE

Rationale: The live and public manifests match byte-for-byte, both align with `content-draft/viet/website-preview.json` on module count and exact `(moduleId, scenarioId)` pairs, and the hardened validator now checks the live export seam directly, including site-owned audio reachability. `result.md` and `state.json` are still in their expected pre-finalization states, so the artifact set is ready to finalize once this review is harvested.
