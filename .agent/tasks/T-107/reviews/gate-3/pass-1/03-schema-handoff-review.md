Findings
- None. The 14-cluster Viet relation sample, the notes-based `relation-sample=` anchors, and the sidecar schema all line up with the additive model described in the docs.

Risks
- The main residual risk is implementation-side parsing and validation of the `notes` markers, plus future drift if sidecar relation hints and generated-pack truth ever diverge.

Approval: APPROVE
