# Gate 2 Pass 4 - Accessibility And Readability

Judgment: Bottom chrome still overlaps the in-quiz primary action after feedback reveal. In the live local prototype, selecting `Xin chào` on prompt 1 leaves the `Next prompt` button partially hidden under the fixed dock/search chrome. The hidden mascot panel reduced vertical pressure, but did not clear the CTA after feedback expansion.

Resolved or non-blocking: in-quiz mascot/progress panel is `display: none`; English answer details stay hidden before selection and reveal after answering; feedback copy itself is readable and not clipped; contrast is acceptable for primary, muted, and red text in the rendered prototype.

Approval: BLOCK
