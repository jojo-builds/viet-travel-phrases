# Gate 2 Pass 6 - Offline Practice Data Fit

Judgment: The four mock prompts all require Vietnamese listening/recognition or Vietnamese phrase choice. English choice details are hidden by default and revealed only after selection for the selected/correct path. The first audio path points at existing bundled `native-ios/Resources/Audio/polite-1.mp3`. No runtime AI, external network, or random dependency was found; the prototype uses static prompt data and a local `new Audio(...)` path only. Prior blocked user-facing `concept`/prototype labels are no longer visible in the prototype copy.

Approval: APPROVE
