Approval: BLOCK

1. Clarify checklist ownership in `.agent/tasks/T-112/spec.md`. The spec says to leave one exact Viet execution packet, but it only says to prefer one clear operator story across five surfaces. That still leaves room to restate the six-item packet in multiple places, and the current surfaces already do that in `docs/operations/TESTING_RUNBOOK.md`, `docs/operations/APP_STATUS.md`, `docs/operations/CURRENT_BLOCKERS.md`, and `ops/apps/viet.json`. The implementation pass should make `docs/operations/VIET_TESTFLIGHT_EXECUTION_PACKET.md` the sole ordered checklist owner, and the other touched surfaces should only summarize status, blockers, and where to return evidence.
2. Lock `docs/operations/LATEST_VALIDATION.md` out of scope unless new evidence exists. `ops/apps/viet.json` still tells the operator to sync `LATEST_VALIDATION.md`, but `T-112` required outputs omit that file. Without an explicit do-not-edit-unless-new-evidence rule, the pass has a plausible reason to widen beyond the named outputs.
