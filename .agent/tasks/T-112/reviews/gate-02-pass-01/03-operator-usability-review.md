Approval: APPROVE

`docs/operations/VIET_TESTFLIGHT_EXECUTION_PACKET.md` now works as the one front-door packet: it explicitly tells the operator to start from that file instead of reconciling mirrors first, and it owns the exact six-item return packet. The other touched surfaces now read as summaries and routing layers rather than competing checklists: `TESTING_RUNBOOK.md`, `APP_STATUS.md`, `CURRENT_BLOCKERS.md`, and `ops/apps/viet.json`.
