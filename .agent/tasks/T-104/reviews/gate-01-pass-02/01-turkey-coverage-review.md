Approval: BLOCK

The planned closeout is not reflected yet in the reviewed artifacts. The current Turkish CSV still leaves the 4 coffee-shop rows and 2 asking-price rows untranslated, and the 6 small-talk rows still sit in the generic `needs-translation` bucket instead of an explicit later-only deferred bucket. The handoff surfaces also still describe a residual untranslated queue rather than giving every unresolved row or bucket a clear reason and next owner.

Must-fix conditions:
- Translate the 4 coffee-shop rows and 2 asking-price rows.
- Move the 6 small-talk rows into an explicit later-only deferred bucket.
- Update the Turkish handoff docs so every unresolved row or unresolved bucket has an explicit reason and next owner.
