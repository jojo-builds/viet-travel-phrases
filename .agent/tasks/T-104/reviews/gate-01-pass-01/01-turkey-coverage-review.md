Approval: APPROVE

The proposed direction matches the spec's coverage goal: it clears the full currently practical tail of 4 coffee-shop rows plus 2 asking-price rows instead of taking a partial batch, while leaving the 6 small-talk rows as an explicit later-only residual. It also preserves the existing rewrite and native-review handoff posture for non-translation holdouts, which keeps the residual story clean instead of mixing practical rows with unresolved review cases.

Must-fix conditions: none, provided the implementation makes the small-talk residuals explicit rather than leaving them as silent `needs-translation` leftovers, and does not disturb the existing rewrite, localization-check, or expert-review flags.
