Approval: BLOCK

The 6 practical plus 6 later-only split is within scope and matches the current residual queue, but the closeout contract must still explicitly include final `state.json` finalization alongside `result.md`. The spec's done criteria also require every remaining unresolved row to carry an explicit reason and next owner, which means the handoff needs to cover the five review holdouts plus the two rewrite-needed rows as well as the six small-talk leftovers.

Must-fix conditions:
- Finalize `.agent/tasks/T-104/state.json` together with `result.md` after unanimous Gate 3 approval.
- Make the residual handoff explicitly assign reason and next owner for the five holdouts plus the two rewrite-needed rows, not just the small-talk leftovers.
