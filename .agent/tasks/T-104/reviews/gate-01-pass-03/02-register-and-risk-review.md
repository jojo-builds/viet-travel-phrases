Approval: APPROVE

The plan matches the current Turkish tail split and preserves the safety boundaries. Translating the 6 practical rows is the right closure move, and explicitly deferring the 6 small-talk rows keeps the low-priority social tail from being mistaken for unfinished starter utility. It also keeps the caution flags intact for the sensitive rows: medical stays on expert review, the bargaining rows remain contextual-only, and the service and context-sensitive rows stay marked for rewrite or local review instead of being normalized too early.

Must-fix conditions: keep the existing flags, give every unresolved rewrite, native-review, or contextual row an explicit reason and next owner, and do not finalize `state.json` or `result.md` before Gate 3 unanimity.
