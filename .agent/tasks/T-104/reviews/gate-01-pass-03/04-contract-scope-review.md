Approval: APPROVE

The plan matches the spec and the current draft state: it targets the exact 6 unresolved practical rows, explicitly defers the 6 remaining small-talk rows instead of leaving them as silent leftovers, preserves the existing rewrite and expert-review cautions, and keeps the Gate 3 sequencing intact by deferring `done` until unanimous approval plus `state.json` finalization. It also stays within the allowed write surface and covers the required Turkish artifacts.

Must-fix conditions: none. Keep the existing named holdouts visible, and make sure the small-talk deferral is written as an explicit later-only handoff rather than a generic `needs-translation` bucket.
