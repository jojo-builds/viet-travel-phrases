**Findings**
- Family 8 is still over-merged: `Please help me block my bank card` and `Please help me contact my bank` are not the same compact variant. The second line moves to a next-step action instead of restating the card-blocking need, so this should be split.
- Family 10 is still over-merged: `Where is lost and found?` and `Can you call lost and found for me?` are different traveler moments, one location lookup and one contact request. That is not a pure clearer variant pair.

**Approval**
Approval: BLOCK
