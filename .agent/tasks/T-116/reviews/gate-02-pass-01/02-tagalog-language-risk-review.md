Approval: APPROVE

Findings:
- The deferred refusal row stays visibly deferred, with an explicit route back to thanks or the repair lane and no runtime-promotion claim.
- The five direct-pickup rows remain an ordered review-bound cluster, not promoted truth, and the scenario plan ties them to named blocker surfaces.
- The later-only hold rows are still separated from the direct-pickup cluster and carry their own reopen rule, so the boundary stays honest.

Must-fix guidance:
- None. Keep the current labels and stop-condition text unchanged, and do not collapse the later-only holds into the direct-pickup lane.
