Approval: APPROVE

Findings:
- The deferred refusal row is kept visibly deferred, with explicit route-back handling into thanks or the direct repair lane and no promotion claim.
- The hotel/check-in/aircon/card set is clearly framed as an ordered next-pass pickup cluster, not as promoted runtime truth.
- The later-only holds are still separated from the pickup cluster with an explicit unlock rule, so the boundary stays honest.
- The CSV, priority sheet, and scenario plan all agree on the same 24-row contract and status split.

Must-fix guidance:
- Keep the refusal row and the hotel/payment rows labeled as review-bound until native confirmation lands; do not let the prose drift into implying they are already runtime-safe.
