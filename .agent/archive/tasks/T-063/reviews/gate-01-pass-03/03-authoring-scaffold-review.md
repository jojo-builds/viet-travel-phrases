# Gate 1 Pass 3 Authoring Scaffold Review

- reviewer: Laplace (`019da7c0-6a1a-7e50-907c-cf861fa70b6c`)
Approval: APPROVE
- scope: untouched Korean lane plus live queue claim evidence

## Findings

- The coordination evidence removes the only meaningful process ambiguity. `T-063` is the claimed Korean scaffold task, while `T-018` remains historical context only.
- The Korean lane is still only research-ready from an authoring-scaffold standpoint, but that is now the intended edit target rather than a blocker.
- The exact scaffold debt is already well scoped: bargaining-heavy price rows, city-center direction rows, and politeness or show-screen-sensitive lines.

## Recommended edits

- Create `content-draft/korean/phrase-source.csv` as the full row-level scaffold with per-row notes, statuses, and Korea-fit rewrite decisions.
- Create `content-draft/korean/first-wave-priority.csv` as a ranked starter queue with required contract fields.
- Update Korean docs so they point to the new scaffold files instead of describing shortlist creation as still pending.
