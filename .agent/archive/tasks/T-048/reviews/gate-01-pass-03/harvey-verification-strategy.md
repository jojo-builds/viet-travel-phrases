APPROVE

- Forced-failure proof is now in the required execution sequence before broader burn-in claims.
- Survivor-only automation smoke is now explicit in the plan.
- `state.json` now enforces both new verification artifacts as required done evidence.

Reviewer note:
- Non-blocking: include the final active/retired automation statuses in `authoritative-automation-smoke.md`.
