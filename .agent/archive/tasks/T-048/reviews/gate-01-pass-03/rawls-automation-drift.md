APPROVE

- Plan now covers injected proof for both observed write-failure surfaces.
- Plan now requires the survivor-only automation smoke after duplicate retirement.
- `state.json` now enforces both new evidence artifacts as completion prerequisites.

Reviewer note:
- Non-blocking: capture the final TOML statuses for both automation ids in the survivor smoke artifact.
