Verdict: BLOCK

Findings:
- The plan promotes `tagalog-hotel-hostel-5` into the core while still describing `aircon` as a draft-only borrowing that needs expert review. That makes the promoted-core vs holdout boundary ambiguous and leaves the loanword posture unsettled.

Required changes before implementation:
- Move `tagalog-hotel-hostel-5` out of the promoted core, or rewrite it so the loanword posture is fully resolved before promotion.
- Add an explicit rule that any row with unresolved loanword or pronunciation notes stays in the holdout set until that note is settled.

Approval line: I do not approve advancement to implementation.
