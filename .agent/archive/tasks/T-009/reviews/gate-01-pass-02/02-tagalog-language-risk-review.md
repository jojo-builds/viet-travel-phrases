# Verdict
BLOCK

# Findings
- The first-wave shortlist still mixes promoted rows with active risk rows such as `tagalog-polite-basics-4`, `tagalog-directions-1`, `tagalog-hotel-hostel-1`, `tagalog-hotel-hostel-2`, `tagalog-convenience-store-6`, and `tagalog-simple-problems-7`, so the next pass would still have to resolve baseline wording instead of just authoring from a settled core.
- The current notes acknowledge mixed English and pronunciation uncertainty, but they do not yet leave an explicit, settled posture for register, loanwords, and pronunciation that the next authoring pass can treat as stable draft rules.
- Several loanword-heavy rows remain in the promoted area without a clear normalize-or-demote decision, which keeps the Tagalog lane exposed to inconsistent traveler-facing tone and uneven pronunciation guidance.

# Required changes before implementation
- Separate the clean promoted core from rows that still need native or expert judgment, and demote the latter out of the core if they are not normalized.
- Add explicit posture notes for register, loanwords, and pronunciation so the next pass starts from settled draft rules instead of open-ended ambiguity.
- Rewrite or clearly flag the remaining weak first-wave rows before treating the shortlist as implementation-ready.

# Approval line
I do not approve advancement to implementation.
