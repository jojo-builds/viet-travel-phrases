APPROVED

This revision fixes the operator-authority split by making one repo-owned `docs/operations` packet the single ordered checklist, while keeping `APP_STATUS.md`, `CURRENT_BLOCKERS.md`, and `LATEST_VALIDATION.md` in their proper status/evidence roles only. It also preserves the key honesty boundaries for the next iPhone pass: build `1.0.0 (3)` is still the latest installable artifact, builds `(4)` and `(5)` remain in-flight references only, and no durable purchase/restore/device proof is claimed beyond the `2026-04-16` snapshot.
