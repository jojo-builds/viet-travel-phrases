WITHHOLD

- The plan must explicitly commit to filling `target_text`, `pronunciation`, `notes`, and `status` for the translated rows in `content-draft/japanese/phrase-source.csv`.
- Rank 23 must be rewritten before translation rather than passed through as the existing English source.
- The execution plan must explicitly include closure artifacts and validation: `result.md`, review evidence for all three gates, CSV parsing, and non-empty `first-wave-priority.csv`.

Blocking change before execution:
- restate the execution plan with those deliverables and validations included
