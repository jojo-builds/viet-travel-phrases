Approval: APPROVE

Findings
- The Thai scaffold is materially complete for handoff. `phrase-source.csv` has the shared seam plus Thai-specific supplemental rows and a repair mini-set, and `first-wave-priority.csv` gives a concrete ranked start point.
- The supporting notes stay aligned with prep-only scope, Thai script and pronunciation caution, and the repair-first authoring posture.
- With `gate-03-pass-03` saved and the final state synced, the remaining gap is administrative, not structural.

Required changes before complete
- Save `gate-03-pass-03` with the 4 review artifacts.
- Sync `result.md`, `state.json`, `queue-index.json`, and `locks.yaml` to the final approved state.

Approved rationale
- The next translation task can start immediately from the ranked shortlist without another orientation pass.
- The scaffold is strong enough, scoped correctly, and ready to hand off as durable prep truth once the final evidence and state files are aligned.
