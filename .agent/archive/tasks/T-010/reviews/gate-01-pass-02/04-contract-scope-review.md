Approval: APPROVE

Findings
- The task contract is consistent with a prep-only Thai scaffold task and does not require runtime wiring.
- `content-draft/thai/phrase-source.csv` exists and is non-empty; it gives the next pass a usable source scaffold.
- `content-draft/thai/first-wave-priority.csv` exists and is non-empty; it provides a concrete ranked starting point.
- `content-draft/thai/README.md`, `source-notes.md`, and `research-backlog.md` make the lane actionable without another orientation pass.

Required changes before proceed
- None for Gate 1.

Approved rationale
- The Gate 1 goal is satisfied: the Thai lane now has the required reusable scaffold and ranked first wave, the scope stays inside prep-only bounds, and the next translation task can start directly from the new files.
