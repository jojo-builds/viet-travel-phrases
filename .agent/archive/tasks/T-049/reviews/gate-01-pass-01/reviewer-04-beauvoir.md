APPROVE

No smaller non-`queue_tool.py` fix is evident. The helper hard-binds its roots from `Path(__file__).resolve()` at import time in `E:\AI\SpeakLocal-App-Family\.agent\queue_tool.py:23`, so any direct `py .agent\queue_tool.py ...` launch stays vulnerable to the junction collapsing back to `E:\AI\Viet-Travel-Phrases` regardless of cwd; the audit documents that directly in `E:\AI\SpeakLocal-App-Family\.agent\tasks\T-049\logs\root-cause.md:5`. The shared queue surfaces still tell runs to invoke the helper directly for claim/heartbeat/finish in `E:\AI\SpeakLocal-App-Family\.agent\CODEX_DESKTOP_AUTOMATION_PROMPT.txt:18`, `E:\AI\SpeakLocal-App-Family\.agent\QUEUE_START.md:42`, and `E:\AI\SpeakLocal-App-Family\.agent\AUTOMATION.md:77`, while the retained clone evidence already shows alias-path write and lock failures from a nominal canonical-root run in `C:\Users\Administrator\.codex\automations\task-queue-10-3\memory.md:12` and `C:\Users\Administrator\.codex\automations\task-queue-10-3\memory.md:20`. That is complete enough to proceed with the prompt/docs wrapper as the smallest in-scope fix; cwd-only normalization, memory cleanup, or skill tweaks would not satisfy the definition of done. Missing proof is only patch/verification-stage proof that the wrapper replaces direct helper calls for claim, heartbeat, and finish and keeps paths canonical when launched from a non-canonical cwd.
