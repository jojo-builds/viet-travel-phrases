BLOCK

`root-cause.md` proves the helper’s `Path(__file__).resolve()` binding is sufficient to collapse queue writes to the alias (`queue_tool.py:23-30`; `root-cause.md:40-64`), and `memory.md` shows `task-queue-10-3` still hit alias-path warnings/locks even when the run reported starting from `E:\AI\SpeakLocal-App-Family` (`memory.md:8-13`, `memory.md:17-22`). That means the audit has not actually proven alias cwd or prompt-relative invocation were contributors in the failing runs, because the prompt/docs already required canonical cwd before `py .agent\queue_tool.py ...` (`CODEX_DESKTOP_AUTOMATION_PROMPT.txt:2-3,17-18`; `QUEUE_START.md:9-10,42`; `AUTOMATION.md:7-8,75-78`). It also does not fully rule broad-startup or stale-memory drift in or out; it only shows they are unnecessary to reproduce, but there is no captured runner trace or config evidence showing whether `$orchestrate-speaklocal-family` or automation memory actually loaded before failure. The proposed wrapper fix looks targeted to the proven `resolve()` path-binding bug (`root-cause.md:103-110`), but the causal statement in `root-cause.md:95-101` over-attributes cwd/startup drift without that missing proof.
