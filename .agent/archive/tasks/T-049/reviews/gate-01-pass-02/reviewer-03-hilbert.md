APPROVE

The updated audit now cleanly isolates the proven Gate 1 cause to launch-time path binding: `queue_tool.py` derives all queue surfaces from `Path(__file__).resolve()` (`E:\AI\SpeakLocal-App-Family\.agent\queue_tool.py:23`), the junction collapses canonical spelling to the alias, and direct helper invocation preserves that bad binding (`E:\AI\SpeakLocal-App-Family\.agent\tasks\T-049\logs\root-cause.md:5`, `:24`, `:40`, `:135`). It also fixes the prior over-attribution by treating cwd as unproven rather than necessary and explicitly rules memory and broad-startup drift out as primary causes for this bug reproduction (`E:\AI\SpeakLocal-App-Family\.agent\tasks\T-049\logs\root-cause.md:128`). The alias-shell wrapper prototype is enough for Gate 1 to show the proposed fix is appropriately targeted and feasible in scope, while correctly deferring `replace_with_retry` and other behavioral checks to post-patch verification instead of claiming them settled here (`E:\AI\SpeakLocal-App-Family\.agent\tasks\T-049\logs\root-cause.md:87`, `:145`, `:156`).
