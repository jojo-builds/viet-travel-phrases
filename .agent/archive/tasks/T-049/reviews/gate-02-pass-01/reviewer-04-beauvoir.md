APPROVE

The current queue surfaces now make the canonical wrapper, not automation cwd or memory, the startup authority: `E:\AI\SpeakLocal-App-Family\.agent\CODEX_DESKTOP_AUTOMATION_PROMPT.txt:17`, `E:\AI\SpeakLocal-App-Family\.agent\QUEUE_START.md:39`, and `E:\AI\SpeakLocal-App-Family\.agent\AUTOMATION.md:29` all replace direct `py .agent\queue_tool.py ...` usage with `Invoke-SpeakLocalQueueTool`, explicitly call out alias collapse risk, and route `claim-next` / `heartbeat` / `finish` through the wrapper instead (`CODEX_DESKTOP_AUTOMATION_PROMPT.txt:51`, `QUEUE_START.md:76`, `AUTOMATION.md:111`). The stale-memory and broad-family drift vectors are also fenced off by the queue-run instructions not to read automation memory or follow `$orchestrate-speaklocal-family` during normal startup in `CODEX_DESKTOP_AUTOMATION_PROMPT.txt:4`, `:15`, `QUEUE_START.md:30`, and `AUTOMATION.md:25`.

Queue safety is not weakened because the docs still preserve helper enforcement as the only claim/finish path, and the helper still enforces lock mutation, runtime claim eligibility, session validation, and done prerequisites in `E:\AI\SpeakLocal-App-Family\.agent\queue_tool.py:846`, `:887`, `:1076`, and `:1123`. That matches the stated patch intent in `E:\AI\SpeakLocal-App-Family\.agent\tasks\T-049\logs\root-cause.md:116`.
