APPROVE

`E:\AI\SpeakLocal-App-Family\.agent\CODEX_DESKTOP_AUTOMATION_PROMPT.txt:17-19,21-52`, `E:\AI\SpeakLocal-App-Family\.agent\QUEUE_START.md:39-42,44-76`, and `E:\AI\SpeakLocal-App-Family\.agent\AUTOMATION.md:29-32,34-61,105-112,188-194` now consistently replace direct `py .agent\queue_tool.py ...` launches with the canonical `Invoke-SpeakLocalQueueTool` wrapper, so queue-helper calls no longer depend on automation cwd or alias-path startup. Those same surfaces also explicitly demote automation `memory.md` from startup authority and exclude broad-family startup drift (`CODEX_DESKTOP_AUTOMATION_PROMPT.txt:4-5,15`, `QUEUE_START.md:30-37`, `AUTOMATION.md:23-27`), which aligns with the narrowed root-cause and fix framing in `E:\AI\SpeakLocal-App-Family\.agent\tasks\T-049\logs\root-cause.md:116-126`.

Queue safety and helper enforcement are still preserved, not bypassed: the wrapper only rebinds the helper’s path globals before calling `main`, while `E:\AI\SpeakLocal-App-Family\.agent\queue_tool.py:1201-1235` still enforces allowed roots, `:967-1059` still owns claim/repair/index rewrite behavior, and `:1076-1156` still enforces session validation plus finish prerequisites. That is enough for Gate 2 to advance.
