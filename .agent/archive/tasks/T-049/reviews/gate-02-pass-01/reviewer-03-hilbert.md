APPROVE

The patched queue surfaces now make `Invoke-SpeakLocalQueueTool` the only supported launcher, explicitly forbid direct `py .agent\queue_tool.py ...` calls, and rebind `SCRIPT_PATH` / `REPO_ROOT` / queue paths to `E:\AI\SpeakLocal-App-Family`, which removes direct dependence on automation cwd or alias-path startup (`E:\AI\SpeakLocal-App-Family\.agent\CODEX_DESKTOP_AUTOMATION_PROMPT.txt:17`, `E:\AI\SpeakLocal-App-Family\.agent\QUEUE_START.md:39`, `E:\AI\SpeakLocal-App-Family\.agent\AUTOMATION.md:29`, `E:\AI\SpeakLocal-App-Family\.agent\tasks\T-049\logs\root-cause.md:116`). They also now demote automation `memory.md` to debug-only and explicitly suppress broad-family skill/preflight drift during queue startup (`CODEX_DESKTOP_AUTOMATION_PROMPT.txt:14`, `QUEUE_START.md:30`, `AUTOMATION.md:23`).

Queue safety and helper enforcement stay intact because claim, heartbeat, and finish still flow through `queue_tool.py`, which continues to enforce locks, session validation, finish prerequisites, and repo-root checks in `main()` rather than moving those decisions into the wrapper (`AUTOMATION.md:105`, `E:\AI\SpeakLocal-App-Family\.agent\queue_tool.py:967`, `:1088`, `:1116`, `:1201`).
