# Gate 2 Authoring Scaffold Review

The lane is now authoring-ready enough for the next translation pass to work directly from it. `README.md` names the file roles clearly, `phrase-source.csv` is the direct row-level filter surface, `first-wave-priority.csv` mirrors the same bounded 16/6/2 contract, `rewrite-notes.md` explains the tightened source wording, and `scenario-plan.json` now keeps quick phrases inside the core.

Decision: APPROVE

Conditions:
- Keep the next translation pass scoped to `status=first-wave-core` in `phrase-source.csv`.
- Treat `first-wave-holdout` and `first-wave-deferred` rows as visible follow-up queue only, not part of the direct authoring pack.
- Preserve the prep-only boundary and helper-only pronunciation posture while translation work proceeds.
