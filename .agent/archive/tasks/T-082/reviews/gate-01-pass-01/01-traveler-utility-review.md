Approval: APPROVE

Findings:
- The lane supports an honest premium-follow-on pass.
- `tagalog-directions-1` is the strongest non-core row because it solves the map-showing need directly and is already treated as premium follow-on rather than deferred.
- The next strongest non-core rows are the hotel-arrival cluster (`tagalog-hotel-hostel-1`, `tagalog-hotel-hostel-2`, `tagalog-hotel-hostel-5`) plus the payment rows (`tagalog-convenience-store-6`, `tagalog-grab-taxi-7`).
- `tagalog-simple-problems-7` is useful, but its register and pronoun posture make it weaker than the hotel, directions, and payment rows for the active next-pass queue.
- `tagalog-polite-basics-4` must remain deferred.

Recommended direction:
- Center the pass on `tagalog-directions-1`, then the hotel-arrival rows, then payment rows.
- Leave `tagalog-simple-problems-7` outside the active first-wave queue until register review is stronger.
- Keep the refusal row deferred.

Risks:
- Hotel and payment rows still lean on borrowings.
- Over-expanding the active queue would turn holdout reduction into cosmetic relabeling.
- `tagalog-simple-problems-7` stays brittle until register is better settled.
