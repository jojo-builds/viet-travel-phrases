Approval: APPROVE

Findings:
- The main pass preserves an honest risk posture: the active queue is now clearly `16 / 5 / 1`, and the narrowed holdout set is still labeled as language-risk-bound rather than core-ready.
- `tagalog-directions-1` is treated as the strongest active follow-on row, but it still carries a native-review caution before promotion.
- The hotel, check-in, aircon, and card rows remain in `premium-follow-on` with explicit loanword/payment review flags.
- `tagalog-polite-basics-4` remains deferred, and the later escalation/cash rows were parked in the broader backlog instead of being silently promoted.

Recommended direction:
- Keep the next pass focused on the five active holdouts plus the deferred refusal row, with `tagalog-directions-1` first if a map baseline is needed.
- Do not move the refusal row out of deferred until refusal-tone review lands.
- Revisit the parked escalation and cash rows only after the active follow-on cluster is exhausted.

Risks:
- Loanword posture is still unsettled for reservation, check-in, aircon, and card wording.
- `tagalog-directions-1` is improved but still not freeze-ready without native validation.
- Pronunciation remains helper-only draft guidance.
