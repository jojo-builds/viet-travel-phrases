Approval: APPROVE

Findings:
- The post-edit pack is internally coherent.
- `README.md`, `source-notes.md`, `rewrite-notes.md`, `risk-review.md`, `research-backlog.md`, and `tagalog-v2-first-wave.csv` now agree on a `16 / 5 / 1` active surface with `2` rows parked outside the active first-wave queue.
- The narrowed queue is backed by real row-level changes, not cosmetic relabeling: `tagalog-simple-problems-7` and `tagalog-grab-taxi-7` were moved out of the active handoff and now show as `drafted`, while `tagalog-v2-first-wave.csv` drops them from the merged pack entirely.
- The remaining active follow-on rows stay clearly differentiated and review-bound.

Recommended direction:
- Proceed with the narrower `5 + 1` active follow-on queue.
- Keep the parked escalation and cash rows outside the active pack until a later pass justifies re-entry.
- Preserve this honesty by moving only rows with a clear next-pass rationale.

Risks:
- The pack would become misleading if the parked rows were treated as hidden progress instead of explicit backlog.
- The current coherence depends on the distinction between active follow-on rows and later drafted backlog staying visible in both prose and CSV surfaces.
