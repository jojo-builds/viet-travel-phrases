Reviewer: Codex
Role: contract and scope
Gate: 01
Pass: 01
Verdict: APPROVED

The planned packet-only fix stays inside the allowed write scope and directly addresses the task objective from T-034: Step 2 becomes the source of truth for the selected preview and TestFlight builds, and Step 4 / Step 5 then follow that selection instead of a stale hard-coded TestFlight build. That is sufficient for the narrow follow-up, with broader docs or `ops/apps/viet.json` only needing touch if the packet edit exposes a real truth mismatch.

Approval: APPROVE
